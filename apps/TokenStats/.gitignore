# Xcode build output (from xcodebuild -derivedDataPath build)
build/

# Scratch space (DerivedData, critiques, logo experiments)
.scratch/

# Xcode per-user state
xcuserdata/
*.xcuserstate

# Per-checkout development signing. Copy the tracked example and edit locally.
/Config/Signing.local.xcconfig
